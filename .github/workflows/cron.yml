name: Call DLT
on:
  schedule:
    - cron: '*/5 * * * *'  # ทุก 5 นาที

jobs:
  call:
    runs-on: ubuntu-latest
    steps:
      - name: Call URL
        run: curl https://kanchanaburi.dlt.go.th/TLnotication/check_and_send_mail
